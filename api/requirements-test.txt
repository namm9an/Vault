# Minimal deps for running unit tests on the host (no asyncpg/asyncio DB needed).
# Tests mock the DB session, so asyncpg is deliberately excluded here.
fastapi==0.115.0
sqlalchemy[asyncio]==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
pytest==8.3.3
pytest-asyncio==0.24.0
anyio==4.6.0
